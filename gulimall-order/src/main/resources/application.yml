spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/gulimall_oms
    username: root
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 9000
